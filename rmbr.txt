grosse modif sur la fonction get_map, pas le temps de test mais je fetch les args des textures ET la map en meme temps (tres douteux)
construction en cours sur textures.c et parsing.c

void render(t_data *data)
-->	// get_texture(data, "NO");

./textures/north_wall.xpm

faire plus de test pour le parsing RGB floor and ceiling

parsing pete complet si map totalement decaler par des whitespace (si colonne completement vide sa pete)
--> gestion des maps a forme diversent, pas juste check les extremitté

re-check sujet
faire plus de test pour les textures et voir si correcteur peuvent mettre leur propre texture

